user:
  locations:
    location1:
      - ip.address1
      - ip.address2
    location2:
      - ip.address1
      - ip.address3
      - ip.address4

features:
  metrics: true

metrics_api_url: 'https://example.com'

stacks:
  storage_root: spec/fixtures
  ocfl_root: spec/fixtures/ocfl
